"""
// AICODE-NOTE: Пакет фонового воркера на Redis/RQ для запуска агентов.

Назначение:
- Инициализация очереди и соединения с Redis (`queue.py`).
- Определение задач (`tasks.py`), включая запуск агента и возврат финального результата.
- Точка входа воркера (`run.py`).

Переменные окружения:
- `REDIS_URL` — строка подключения к Redis (`redis://localhost:6379/0`).
- `AGENT_QUEUE` — имя очереди (по умолчанию `agents`).
- `AGENT_JOB_TIMEOUT` — таймаут задачи (секунды, по умолчанию `900`).
- `AGENT_RESULT_TTL` — TTL результата (секунды, по умолчанию `600`).
"""


